{
  "name": "genopoisk",
  "version": "1.0.0",
  "private": true,
  "description": "Genopoisk — Telegram Mini App for movie search and playback",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "lint": "eslint api/ public/js/ --no-error-on-unmatched-pattern || true",
    "lint:check": "eslint api/ public/js/ --no-error-on-unmatched-pattern",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vitest": "^1.0.0"
  }
}
